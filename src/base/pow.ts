
/**
 * @title pow
 * @description 次冥
 * @param base {number} 基数
 * @param exponent {number} 指数
 * @returns {number}
 */
export function pow(base: number, exponent: number): number {
	return Math.pow(base, exponent)
}

// console.log(Math.pow(2, 0.5))
// console.log(Math.pow(2, 1.5))
// console.log(Math.pow(2, 0.5) * 2)