/**
 * @title isEven
 * @description 偶数
 * @param num {number}
 * @returns {boolean}
 */
export const isEven = (num: number) => num % 2 === 0

/**
 * @title isOdd
 * @description 奇数
 * @param num {number}
 * @returns {boolean}
 */
export const isOdd = (num: number) => num % 2 === 1